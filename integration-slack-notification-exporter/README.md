# integration-slack-notification-exporter

Custom data-exporter for [Rapax](https://rapax.io) network monitoring.

## Overview

Integration generated from the `data-exporter` template.

### Supported Event Categories

- Network device events

**Total definitions:** 0 trap/event types

## Quick Start

```bash
# Build the image
docker build -t integration-slack-notification-exporter:latest .

# Run the container
docker run -d \
  --name integration-slack-notification-exporter \
  --hostname "$(hostname)" \
  --network rapax-dev-network \
  -v "$RAPAXHOME/lib:/opt/rapax/lib:ro" \
  -v "$RAPAXHOME/etc:/opt/rapax/etc:ro" \
  -v "$RAPAXHOME/logs:/opt/rapax/logs" \
  -v /etc/machine-id:/etc/machine-id:ro \
  -e REDIS_HOST=rapax-redis \
  -e "REDIS_PASSWORD=$REDIS_PASSWORD" \
  integration-slack-notification-exporter:latest
```

## Files

| File | Description |
|------|-------------|
| `bin/slack-notification-exporter-processord.py` | Main processor daemon |
| `Dockerfile` | Container image definition (Python 3.11-slim) |
| `requirements.txt` | Python dependencies |
| `README.md` | This file |

### Volume Mounts

```bash
-v "$RAPAXHOME/lib:/opt/rapax/lib:ro"
-v "$RAPAXHOME/etc:/opt/rapax/etc:ro"
-v "$RAPAXHOME/logs:/opt/rapax/logs"
-v /etc/machine-id:/etc/machine-id:ro
```


## Environment Variables

| Variable | Default | Description |
|----------|---------|-------------|
| `RAPAXHOME` | `/opt/rapax` | Rapax installation directory |
| `PYTHONPATH` | `/opt/rapax/lib` | Python library path |
| `LOG_LEVEL` | `INFO` | Logging verbosity |
| `REDIS_HOST` | `rapax-redis` | Redis server hostname |
| `REDIS_PASSWORD` | — | Redis authentication password |

## Logs

```bash
# View container logs
docker logs integration-slack-notification-exporter

# Tail log file
tail -f $RAPAXHOME/logs/slack-notification-exporter.log
```

## Generated By

- **Template:** `data-exporter`
- **Target system:** Custom
- **Generator:** Bruce Integration Builder
